# Findrix Backend - FastAPI

API para gestión de finanzas personales

## Instalación
1. `python -m venv venv`
2. Activa el entorno
3. `pip install -r requirements.txt`
4. Copia `.env.example` → `.env`
5. `fastapi dev app/main.py`